apiVersion: v1
kind: Service
metadata:
  name: hauling-companion-api
  labels:
    app: hauling-companion-api
spec:
  type: ClusterIP
  selector:
    app: hauling-companion-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
