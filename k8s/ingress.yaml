apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hauling-companion-api
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - hauling-companion-api.jannekeipert.de
      secretName: multi-domain-cert
  rules:
    - host: hauling-companion-api.jannekeipert.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hauling-companion-api
                port:
                  number: 80
